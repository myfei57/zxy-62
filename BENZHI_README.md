基于 Go 实现的综合管廊智慧监控平台项目，一款后端服务，完成管廊舱室环境监测、消防联动与巡检能耗管理。

## 功能

- 舱室与分区管理，舱门、进水阀与喷淋状态跟踪。
- 入廊审批先落盘再放行，断电重启不丢审批。
- 集水坑液位、排水泵顺序与重试去重。
- 烟感、防火阀、通风与分区联动。
- 巡检路线、打卡与逾期提醒。
- 电缆温度滤波与过温判定。
- 用电基线、能耗报表与超用核算。
- 联动审计与文件型持久化。

## 构建

```bash
go build -mod=vendor -o tunnelnet.exe ./cmd/tunnelnet
```

## 运行

```bash
./tunnelnet.exe
```

默认监听 `127.0.0.1:8080`，控制台页面位于 `/cabins`、`/env`、`/patrols`、`/alarms`。

## 环境变量

- `TUNNELNET_ADDR`：监听地址，默认 `127.0.0.1:8080`。
- `TUNNELNET_WEB_DIR`：前端页面目录，默认 `web`。
- `TUNNELNET_DATA_DIR`：文件持久化目录，默认 `data`。
